<project name="plexus-compiler-api" default="jar" basedir=".">
	<description>
		simple example build file
	</description>
	<!-- set global properties for this build -->
	<property name="src" location="src"/>
	<property name="build" location="build"/>
	<property name="dist"  location="dist"/>
	<property name="jarname" value="maven-compile-plugin.jar"/>
	<property name="javadoc" value="javadoc"/>
	<property name="javac.source" value="1.4"/>
	<property name="javac.target" value="1.4"/>
	<property name="classpath" value="" />

	<target name="init">
			<!-- Create the time stamp -->
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}"/>
	</target>

	<target name="compile" depends="init" description="compile the source " >
		<!-- Compile the java code from ${src} into ${build} -->
		<javac source="${javac.source}" target="${javac.target}" srcdir="${src}" destdir="${build}" classpath="${classpath}" />
	</target>

	<target name="jar" depends="compile" description="generate the jar" >
		<!-- Create the distribution directory -->
		<!--<mkdir dir="${dist}/lib"/>-->
		<jar jarfile="./target/${ant.project.name}.jar" basedir="${build}" />
	</target>

	<target name="clean" description="clean up" >
		<!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${build}"/>
		<delete dir="${dist}"/>
	</target>

	<target name="javadoc" description="Produce package javadoc">
		<javadoc destdir="${javadoc}" >
			<fileset dir="src" defaultexcludes="yes">
				<include name="org/**"/>
				<exclude name="org/**/*.html"/>
			</fileset>
		</javadoc>
	</target>
</project>
