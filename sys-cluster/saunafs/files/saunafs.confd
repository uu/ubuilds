# Which roles should we start ?
#
# NOTE: The roles are stopped in *reverse* order of the given list
# so make sure to list sfsmaster *before* the other roles.
#
# Possible values: sfsmaster sfsmetalogger sfschunkserver
#ROLES=""

# If the role sfsmaster fails to start, should we try to run sfsmetarestore -a ?
# Possible values: 0 (no), 1 (yes)
AUTORESTORE=0
