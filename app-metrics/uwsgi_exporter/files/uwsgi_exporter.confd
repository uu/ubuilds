# config file for /etc/init.d/uwsgi_exporter
# run /usr/bin/uwsgi_exporter -h to see available options

UWSGI_EXPORTER_LOGDIR="/var/log/${RC_SVCNAME}"
UWSGI_EXPORTER_OPTS=""

# Logging
#output_log="${UWSGI_EXPORTER_LOGDIR}/uwsgi_exporter.log"
#error_log="${UWSGI_EXPORTER_LOGDIR}/error.log"
