# config file for /etc/init.d/beanstalkd_exporter
# run /usr/bin/beanstalkd_exporter -h to see available options

BEANSTALKD_LOGDIR="/var/log/${RC_SVCNAME}"
BEANSTALKD_OPTS=""

# Logging
#output_log="${BEANSTALKD_LOGDIR}/beanstalkd_exporter.log"
#error_log="${BEANSTALKD_LOGDIR}/error.log"
