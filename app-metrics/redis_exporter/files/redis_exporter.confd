# config file for /etc/init.d/redis_exporter
# run /usr/bin/redis_exporter -h to see available options

REDIS_LOGDIR="/var/log/${RC_SVCNAME}"
REDIS_OPTS=""

# Logging
#output_log="${REDIS_LOGDIR}/redis_exporter.log"
#error_log="${REDIS_LOGDIR}/error.log"
