diff -Naur limewire_orig/build.xml limewire/build.xml
--- limewire_orig/build.xml	2007-09-10 19:19:06.000000000 -0400
+++ limewire/build.xml	2007-09-10 20:54:25.000000000 -0400
@@ -84,10 +84,35 @@
     <target name="test-pkg-lw"
          	description="Tests a single package in limewire/tests.  (Useful for testing outside of the normal directory.)  Requires: -Dpackage="
          	depends="compile, lw-tests.test-package"/>
-    
-    <!-- TODO: implement -->
-	<target name="jar" />
-    
+
+    <target name="jar" depends="compile-src,lw-components.jar">
+        <jar jarfile="${limewire.home}/limewire-core.jar">
+            <fileset dir="${limewire.home}/core/" includes="**/*.class" />
+<!--
+                     includes="**/*.class" />
+                     excludes="*.txt"
+                     excludes="*.xml"
+                     excludes="examples/*"
+                     excludes="native/*"
+                     excludes="run*"/>
+-->
+        </jar>
+        <jar jarfile="${limewire.home}/limewire-gui.jar">
+            <fileset dir="${limewire.home}/gui" includes="com/**/*.class" />
+<!--
+                     excludes="*.txt"
+                     excludes="*.xml"
+                     excludes="lib/*"
+                     excludes="macosx/*"
+                     excludes="native/*"
+                     excludes="root/*"
+                     excludes=xml.war"
+                     excludes=xml.sh"
+                     excludes="run*"/>
+-->
+        </jar>
+    </target>
+
     <!-- TODO: implement -->
     <target name="clean-dist"/>
         
diff -Naur limewire_orig/gui/com/limegroup/gnutella/gui/GUILoader.java limewire/gui/com/limegroup/gnutella/gui/GUILoader.java
--- limewire_orig/gui/com/limegroup/gnutella/gui/GUILoader.java	2007-09-10 19:18:58.000000000 -0400
+++ limewire/gui/com/limegroup/gnutella/gui/GUILoader.java	2007-09-10 21:28:27.000000000 -0400
@@ -268,6 +268,8 @@
         File xmlWar = new File("xml.war");
         File messagesBundlesJar = new File(root,"MessagesBundles.jar");
         File id3v2Jar = new File(root, "id3v2.jar");
+
+	System.out.println(themesJar.getAbsolutePath());
         
         
         if( !isCVS && (!themesJar.exists() || !themesJar.isFile()) )
