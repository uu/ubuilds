--- bin/kibana_orig	2015-02-19 19:09:56.000000000 +0300
+++ bin/kibana	2015-02-25 16:17:00.716332746 +0300
@@ -17,5 +17,5 @@
 NODE=${DIR}/node/bin/node
 SERVER=${DIR}/src/bin/kibana.js
 
-CONFIG_PATH="${DIR}/config/kibana.yml" NODE_ENV="production" exec "${NODE}" ${SERVER} ${@}
+CONFIG_PATH="/etc/config/kibana.yml" NODE_ENV="production" exec "${NODE}" ${SERVER} ${@}

